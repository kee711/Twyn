"use server";
import { generatePostFromAgent, saveDraft, confirmPost, saveDiff } from "@/app/[locale]/(dashboard)/agents/actions";

export { generatePostFromAgent, saveDraft, confirmPost, saveDiff };
